# 乳头区域裁剪策略

## 概述

当前裁剪策略基于 **血管分割 mask 的最高点** 来确定甲襞（乳头）区域，而非依赖传统的边界线检测。核心思想是：血管的管尖（最高点）大致勾勒出甲襞上缘的位置，以此为基准上下扩展形成带状裁剪区域。

## 预处理流程

1. 对每张原始图像，用maskrcnn预测出每一张图片的每一个血管实例 mask 文件（命名格式：`{file_name}_mask_{instance_id}.npy`）
2. 读取所有 mask，找出每个 mask 中血管的最高点（y 坐标最小值）
3. 取所有最高点的 **最小值** 减 `top_offset` 作为裁剪上界
4. 取所有最高点的 **最大值** 加 `bottom_offset` 作为裁剪下界
5. 宽度保持不变（全宽裁剪），只裁剪高度方向

## 关键参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `top_offset` | 200 px | 最靠上的管尖再往上扩展的像素数 |
| `bottom_offset` | 200 px | 最靠下的管尖再往下扩展的像素数 |