# 解决 Nipple Band 训练过拟合 — 实施方案

## 1. 问题诊断

当前 V6 band 训练集仅有 356 张图片，采用 EfficientNet-B0（约 5.3M 参数）网络。模型在 epoch 6 之后表现出严重的**过拟合**症状：

* **Train Acc：** 从 epoch 10 起保持在 97% 以上，并在 epoch 25 达到 100%。
* **Val Acc：** 峰值仅为 63%（在 epoch 6 取得），随后在 47% ~ 61% 之间大幅波动。
* **Val Loss：** 停滞在 1.05 ~ 1.13，不再下降。

> 💡 **核心思路：** 以下三项改进策略互不冲突，可组合使用，旨在通过控制模型容量和引入正则化来缓解过拟合。

---

## 2. 改进策略

### 改动 1: 冻结 Backbone 早期层

#### 原理

EfficientNet-B0 的 `features` 共有 9 个条目（索引 0-8）。由于底层特征（如边缘、纹理）具有通用性，在小数据集上无需重复训练。

| 索引 | 内容 | 参数量占比 |
| --- | --- | --- |
| **0** | Initial Conv (3 $\rightarrow$ 32) | ~1K |
| **1** | Stage 1 MBConv (32 $\rightarrow$ 16) | ~50K |
| **2** | Stage 2 MBConv (16 $\rightarrow$ 24) | ~150K |
| **3** | Stage 3 MBConv (24 $\rightarrow$ 40) | ~250K |
| **4** | Stage 4 MBConv (40 $\rightarrow$ 80) | ~700K |
| **5** | Stage 5 MBConv (80 $\rightarrow$ 112) | ~1M |
| **6** | Stage 6 MBConv (112 $\rightarrow$ 192) | ~2M |
| **7** | Stage 7 MBConv (192 $\rightarrow$ 320) | ~800K |
| **8** | Final Conv (320 $\rightarrow$ 1280) | ~400K |

通过**冻结前 5 个条目（0-4）**，可直接锁定约 40% 的参数量，仅保留后期层（5-8）用于学习任务特定的高级特征。

#### 实现

在 `NippleOnlyModel.__init__` 中进行如下修改，并添加命令行参数 `--freeze_blocks`（默认值 5）：

```python
self.backbone = efficientnet_b0(pretrained=pretrained)
in_features = self.backbone.classifier[1].in_features
self.backbone.classifier = nn.Identity()

# 冻结早期层
for i in range(freeze_blocks):
    for param in self.backbone.features[i].parameters():
        param.requires_grad = False

```

---

### 改动 2: 添加 Dropout

#### 原理

当前模型在 backbone 和 classifier head 之间没有任何 Dropout 保护。EfficientNet 原本自带的 `dropout(p=0.2)` 在自定义的分类器被替换为 `nn.Identity()` 时被意外移除了，需要重新对齐并强化。

#### 实现

在 `NippleOnlyModel.__init__` 中，在全连接分类层（`fc`）和回归层（`reg`）之前插入一个公用的 Dropout 层，并添加命令行参数 `--dropout`（默认值 0.5）：

```python
# __init__ 中定义
self.dropout = nn.Dropout(p=dropout)
self.fc = nn.Linear(in_features, num_classes)
self.reg = nn.Linear(in_features, 1)

# forward 中调用
feat = self.backbone(x)
feat = self.dropout(feat)
return self.fc(feat), self.reg(feat)

```

---

### 改动 3: 标签平滑 (Label Smoothing)

#### 原理

当前使用的 `ordinal_ce_loss` 通过高斯 $\sigma$ 分布对邻近类别赋予软标签（实现有序平滑），但这无法防止模型对正确类别过度自信（即输出概率逼近 1.0）。

引入标签平滑将软标签与均匀分布混合，计算公式如下：


$$\text{soft\_targets} = (1 - \epsilon) \cdot \text{gaussian\_soft\_targets} + \frac{\epsilon}{\text{num\_classes}}$$


这强制让模型始终保留一定的不确定性，从而有效抑制过拟合。

#### 实现

同步修改 `make_ordinal_soft_targets`、`ordinal_ce_loss`、`train_one_epoch` 和 `evaluate` 等函数的签名，添加并传递 `label_smoothing` 参数（默认值 0.1）：

```python
def make_ordinal_soft_targets(labels, num_classes, sigma=1.0, label_smoothing=0.0):
    # ... 原有高斯平滑逻辑 ...
    if label_smoothing > 0:
        soft_targets = (1 - label_smoothing) * soft_targets + label_smoothing / num_classes
    return soft_targets, mask

```

---

## 3. 涉及文件

| 文件 | 改动内容 |
| --- | --- |
| `train_nipple.py` | ① `NippleOnlyModel` 新增 `freeze_blocks` 与 `dropout` 参数；<br>

<br>② `ordinal_ce_loss` 新增 `label_smoothing` 机制；<br>

<br>③ 规范化所有核心函数的调用链和参数传递；<br>

<br>④ 注册 3 个新的 CLI 命令行参数机制。 |
| `eval_nipple_compare.py` | 同步更新 `NippleOnlyModel` 的初始化声明（添加 `dropout` 参数，但确保评估模式下 `eval()` 行为正确）。 |

---

## 4. 验证方法

1. **启动试验：** 在 `bands_v2` 数据集上显式启用新参数运行训练：
```bash
python train_nipple.py --mode band --freeze_blocks 5 --dropout 0.5 --label_smoothing 0.1

```


2. **监控指标：** 观察 TensorBoard 或日志，检查 **Train Acc** 与 **Val Acc** 之间的泛化间隙（Gap）是否明显收窄。
3. **效能对比：** 评估 **Val Acc 峰值**是否能够有效突破原有的 63% 瓶颈，并确认 Val Loss 是否表现出更健康的下降曲线。